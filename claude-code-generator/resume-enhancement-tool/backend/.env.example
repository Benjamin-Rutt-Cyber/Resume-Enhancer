# Resume Enhancement Tool - Environment Configuration Template
# Copy this file to .env and update with your actual values

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# For local development (default)
DATABASE_URL=sqlite:///./resume_enhancement.db

# For production (recommended) - Uncomment and configure:
# DATABASE_URL=postgresql://username:password@localhost:5432/resume_tool
#
# For Docker PostgreSQL:
# DATABASE_URL=postgresql://postgres:postgres@db:5432/resume-enhancement-tool

# ============================================================================
# API KEYS
# ============================================================================

# ANTHROPIC_API_KEY - OPTIONAL (not currently used)
# The AI style preview generation feature has been disabled to eliminate API costs.
# The frontend uses static style selection with predefined descriptions instead.
#
# If you want to re-enable AI-generated style previews in the future:
# 1. Get your API key from: https://console.anthropic.com/
# 2. Set ENABLE_STYLE_PREVIEW_API=true in your .env file
# 3. Uncomment the line below and add your API key
#
# Cost when enabled: ~$0.03 per resume upload (5 API calls)
# Savings from disabling: $3/month → $0/month for typical usage
#
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# ENABLE_STYLE_PREVIEW_API - Set to true to re-enable AI style preview generation
# Default: false (disabled to eliminate API costs)
# ENABLE_STYLE_PREVIEW_API=false

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# SECRET_KEY - REQUIRED - Used for session encryption and security
# IMPORTANT: Generate a strong, random key for production!
#
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Must be at least 32 characters
SECRET_KEY=your-secret-key-minimum-32-characters-long-change-in-production

# DEBUG - Set to False in production for security
# Development: DEBUG=True
# Production: DEBUG=False
DEBUG=False

# ============================================================================
# CORS CONFIGURATION
# ============================================================================

# Allowed origins for CORS (comma-separated)
# Defaults are set in config.py, override here if needed
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# ============================================================================
# FILE STORAGE
# ============================================================================

# Workspace directory for storing uploaded files and generated resumes
# Default: workspace (relative to backend directory)
# For production, consider using an absolute path or environment-specific location
# WORKSPACE_ROOT=workspace
# WORKSPACE_ROOT=/var/app/workspace  # Example production path

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================
#
# Before deploying to production, ensure:
#
# [ ] SECRET_KEY is changed to a strong, random 32+ character string
# [ ] DEBUG is set to False
# [ ] DATABASE_URL points to PostgreSQL (not SQLite)
# [ ] ALLOWED_ORIGINS includes only your production domain(s)
# [ ] File permissions are secure (chmod 600 .env)
# [ ] .env is added to .gitignore (never commit to version control)
# [ ] ANTHROPIC_API_KEY is set ONLY if ENABLE_STYLE_PREVIEW_API=true (optional)
#
# NOTE: Style preview API is disabled by default to eliminate costs.
#       Current API usage: $0/month
#
# ============================================================================

# Additional configuration options can be added here as needed
